{
  "name": "dealership-contact-automation-ui",
  "version": "1.0.0",
  "type": "module",
  "description": "Frontend UI for dealership contact automation system - CDN-based Vue 3 application",
  "scripts": {
    "dev": "python3 -m http.server 8000",
    "start": "node server.cjs",
    "serve": "python3 -m http.server 8000"
  },
  "dependencies": {
    "express": "^5.1.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  },
  "notes": {
    "architecture": "CDN-based Vue 3 application",
    "build": "No build step required - loads Vue from CDN",
    "dependencies": "Only Express server for local development",
    "frontend_tech": "Vue 3 (CDN), vanilla JavaScript, WebSocket client"
  }
}
